{
  "frontend/src/pages/Home.tsx": "Home page with six sections (parallax hero, about preview, services cards, products horizontal scroll, events dark section, CTA). Fixed Rules of Hooks violations by extracting `ServiceCard` and `EventCard` as standalone components so `useScrollReveal` is called at the top level. Also replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/pages/About.tsx": "About page with magazine-style asymmetrical layout including hero, vision/mission, philosophy, values grid, horizontal timeline, and team culture. Fixed Rules of Hooks by extracting `ValueCard` as a standalone component, replaced `flex-shrink-0` with `shrink-0`, and fixed optional chaining on `parentElement`.",
  "frontend/src/pages/Services.tsx": "Services page with alternating split-screen service sections and vertical process timeline. Fixed Rules of Hooks by extracting `ProcessStep` as a standalone component so `useScrollReveal` is called at the top level of a component.",
  "frontend/src/pages/Products.tsx": "Products page with premium product showcase blocks and comparison table. Fixed Rules of Hooks by extracting `ProductBlock` as a standalone component, and replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/pages/Events.tsx": "Events page with parallax banner, category filter, and masonry card grid. Cleaned up unused `useState` import duplication and ensured no hooks are called inside callbacks.",
  "frontend/src/pages/Careers.tsx": "Careers page with animated gradient culture section, benefits grid, accordion open positions, and application CTA. Complete standalone implementation with no hooks-in-callbacks violations.",
  "frontend/src/pages/Gallery.tsx": "Gallery page with masonry grid, category filter (All/Events/Products/Office/Webinars), and hover zoom/overlay effects. Complete standalone implementation.",
  "frontend/src/pages/Contact.tsx": "Contact page with split-screen layout featuring a glass-style form (left) and stylized map placeholder with contact info (right). Integrates with backend via `useSubmitContact` mutation and shows inline success confirmation.",
  "frontend/src/components/GlassCard.tsx": "Reusable glass morphism card component with backdrop blur, translucent background, soft shadow, and rounded corners. Now accepts an optional `style` prop (merged with internal cursor style) in addition to `className`, `dark`, and `onClick`."
}